TO BE DONE EVERY TIME

git checkout main
git fetch upstream
git fetch upstream
git checkout DevBranch
git pull
git merge main


#if you create a newfile locally and want to add it
git add <NewFile>
git commit -m "some comment"
git push origin <BranchName>


